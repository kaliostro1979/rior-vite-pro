export const ROUTES = {
  home: '/',
  steps: '/steps',
  step: '/steps/step/:id',
  success: '/success',
  userPage: '/api/design-requests/:slug/',
  globalComponents: '/global-components'
};

export const STEPS = {
  floorPlan: 1,
  design: 2,
  details: 3,
};
